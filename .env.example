# ========================================
# Self-Hosted Analytics Stack - Environment Variables
# ========================================
#
# SECURITY WARNING: These are example values. You MUST change them!
# 
# Quick Setup:
# 1. Copy this file: cp .env.example .env (or copy manually on Windows)
# 2. Edit .env and replace all values with strong, unique values
# 3. Generate secure random values using:
#    - Windows PowerShell: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
#    - Linux/Mac: openssl rand -hex 32
#
# ========================================

# ----------------------------------------
# PostgreSQL Database Password
# ----------------------------------------
# Used by: PostgreSQL, n8n, Metabase
# Description: Master password for the PostgreSQL admin user
# Security: Use a strong, unique password (minimum 20 characters recommended)
#
POSTGRES_PASSWORD=INSECURE_DEFAULT_CHANGE_ME

# ----------------------------------------
# Metabase Encryption Secret Key
# ----------------------------------------
# Used by: Metabase
# Description: Encrypts sensitive data like database credentials in Metabase
# Security: Must be at least 16 characters. Once set, don't change it or you'll lose access to encrypted data
# Generate: openssl rand -hex 16 (or use PowerShell command above)
#
MB_ENCRYPTION_SECRET_KEY=INSECURE_DEFAULT_CHANGE_ME

# ----------------------------------------
# n8n Encryption Key
# ----------------------------------------
# Used by: n8n
# Description: Encrypts credentials and sensitive data in n8n workflows
# Security: Must be at least 16 characters. Once set, don't change it or you'll lose access to encrypted credentials
# Generate: openssl rand -hex 16 (or use PowerShell command above)
#
N8N_ENCRYPTION_KEY=INSECURE_DEFAULT_CHANGE_ME

# ========================================
# NOTES:
# ========================================
#
# - All services are bound to localhost (127.0.0.1) by default
# - This means they're only accessible from your computer, not from the network
# - Even with default passwords, localhost binding provides basic protection
# - However, you MUST change these if:
#   * Exposing services to the internet
#   * Using a reverse proxy
#   * Accessing from other computers on your network
#
# - Once you set encryption keys (MB_ENCRYPTION_SECRET_KEY and N8N_ENCRYPTION_KEY),
#   DO NOT CHANGE THEM or you will lose access to encrypted data
#
# - Keep this .env file secure and never commit it to version control
#   (it's already in .gitignore)
#
# ========================================
