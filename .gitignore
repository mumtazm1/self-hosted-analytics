# Environment variables
.env

# Shared data directory (but keep the directory structure)
shared/*
!shared/.gitkeep

# Backup files (but keep the directory structure)
backups/*.sql
backups/*.dump
backups/*.gz
!backups/.gitkeep

# Docker volumes (if using local bind mounts)
postgres_data/
n8n_data/

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/